n = ANY NUMBER
src = 'A'
des = 'B'
aux = 'C'

def move_disks(n, src, des, aux):
    if is state legal and is state expected:
        if n == 1:
            print(f"Move disk 1 from {src} to {des}")
            return
        move_disks(n - 1, src, aux, des)
        print(f"Move disk {n} from {src} to {des}")
        move_disks(n - 1, aux, des, src)
    elif is state legal and is not expected:
        newsoultion = rerun script with new parameters:
    else:
        print("Error: Illegal state encountered")
        if ground has disk:
            if option ground1:
                'best fit'
            else then its option ground2:
                'first fit'
        if formation has disk:
            if option formation1:
                'bubble'
            if option formation2:
                'stack'
            else:
                'queue'

move_disks(n, src, des, aux)